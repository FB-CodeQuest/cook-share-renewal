@import "@/styles/variables";
.signupHeader{
  padding: 16px 24px;
  &__container{
    display: flex;
    align-items: center;
  }
  &__back{
    display: flex;
    justify-content: center;
    align-items: center;
    width: 60px;
    height: 60px;
    padding:10px;
    background-color: $label-bg;
    font-size: 24px;
    border-radius: 50%;
    font-weight: 500;
  }
}